html.md
# HTML

HTML is used to create the structure of a webpage.
Add HTML notes
css.md
# CSS

CSS is used to add colors and style to a webpage.
## CSS Syntax

CSS rules have selectors and properties.
# Simple CSS Example

```css
p {
  color: blue;
}
## CSS Properties

Properties define how elements look, such as color and size.
## CSS Comment Example

```css
/* This is a CSS comment */
## CSS Selectors

Selectors choose which HTML elements to style.
## CSS Color Example

```css
h1 {
  color: red;
}
